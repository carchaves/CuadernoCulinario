package dev.raflos.cocina.ui.theme

import androidx.compose.ui.text.font.FontFamily

/** Aproximación con familias del sistema (serif/sans/mono) a las tipografías del diseño
 * (Fraunces, Instrument Serif, Inter, DM Sans, JetBrains Mono) sin depender de Google Fonts
 * descargables en runtime. */
val SerifFamily = FontFamily.Serif
val SansFamily = FontFamily.SansSerif
val MonoFamily = FontFamily.Monospace
