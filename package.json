{
  "name": "cocina-app-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "app",
    "desktop-server"
  ],
  "scripts": {
    "dev": "npm run dev -w app",
    "build:app": "npm run build -w app",
    "android:sync": "npm run build:app && cd app && npx cap sync android",
    "android:build": "npm run android:sync && cd app/android && ./gradlew assembleDebug",
    "android:install": "adb install -r app/android/app/build/outputs/apk/debug/app-debug.apk",
    "android:run": "npm run android:build && npm run android:install && adb shell am start -n dev.raflos.cocina/.MainActivity",
    "desktop": "npm run build:app && npm run start -w desktop-server"
  }
}
